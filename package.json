{
  "name": "@f/changed-paths",
  "description": "Compute the deep paths that have changed between two, presumed immutable, objects",
  "repository": "git://github.com/micro-js/changed-paths.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
